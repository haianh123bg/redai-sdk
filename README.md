# redai-sdk

Monorepo for multiple TypeScript SDKs. The packages are `@redonvn/mst-sdk` and `@redonvn/neac-verify-sdk`.

## Structure

- `packages/mst-sdk` - MST lookup SDK

## Scripts

- `npm run build` - build all packages
- `npm run clean` - clean all packages
